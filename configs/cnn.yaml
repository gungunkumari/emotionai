seed: 42

dataset:
  image_size: 224
  batch_size: 32
  num_workers: 0

training:
  epochs: 2
  learning_rate: 0.0001
  weight_decay: 0.0001

optimizer:
  name: adamw

scheduler:
  name: cosineannealinglr

model:
  name: cnn
  num_classes: 7
  checkpoint: models/checkpoints/best_cnn.pth